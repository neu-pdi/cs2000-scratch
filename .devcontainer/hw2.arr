fun tick(sec :: Number) -> Number:
  doc : "returns a number to 
  if sec < 59:
    sec + 1
  else:
    0
  end
end

    
   
print(tick(25))

#Problem 2
import image
  
fun sec-to-img(sec :: Number) -> Number:
